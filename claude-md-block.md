# org 经验组织系统

你有一套全局经验组织系统(经验池 `~/.claude/org/`,协议 skill `org`,会话启动另有 hook 注入提示):

- **沉淀**:对话中发现高置信度经验(结论/踩坑/验证过的方案/决策)时,提出建议条目,用户确认后写入 `~/.claude/org/topics/<话题>/experience.md`
- **调度**:任务先读 `~/.claude/org/INDEX.md` 匹配角色(`agents/`);命中 spawn 并注入关联话题经验;无匹配提交角色提案,批准后创建
- **铁律**:所有写入(沉淀/建角色/晋升/改注册表)必须先提案、等用户明确确认,禁止静默落盘
- 用户说"沉淀/分工/派agent/建角色/组织agent" → 走 `org` skill
